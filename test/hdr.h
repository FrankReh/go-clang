// Essentially a blank header file. libclang will use hdr.h from unsaved
// buffers as long as it finds the header file in the file system first. So
// this file enables testing of code from header files that exist purely in the
// unsaved buffers passed to libclang.
